// Temporary diagnostic startup script for resource extraction.
Debug.message("=== KAZESHIRO API DUMP BEGIN ===");

Debug.message("--- Storages members ---");
for (var name in Storages) {
    Debug.message("Storages." + name);
}

Debug.message("--- System members ---");
for (var name in System) {
    Debug.message("System." + name);
}

Debug.message("--- global classes ---");
for (var name in global) {
    Debug.message("global." + name);
}

Debug.message("=== KAZESHIRO API DUMP END ===");
System.terminate();
